{
  "name": "the-primitive",
  "version": "1.0.0",
  "private": true,
  "description": "A follow-on article experience for the Harness Series.",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1"
  },
  "devDependencies": {
    "vite": "^8.0.16"
  }
}
