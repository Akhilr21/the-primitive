{
  "name": "agi-loading",
  "version": "1.0.0",
  "private": true,
  "description": "AGI Loading, an interactive field-note reader for harness engineering articles.",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1"
  },
  "devDependencies": {
    "vite": "^8.0.16"
  }
}
